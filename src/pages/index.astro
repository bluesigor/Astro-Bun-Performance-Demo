---
import AppIsland from "../components/AppIsland.jsx";
import PerfIsland from "../components/PerfIsland.jsx";
import AppIslandVue from "../components/vue/AppIslandVue.vue";
---

<header>
  <h1>Astro + Bun Performance Demo</h1>
  <p class="subtitle">
    This page is rendered as static HTML by Astro. Only the interactive parts
    below hydrate.
  </p>
</header>

<section class="explain">
  <h2>Why Astro?</h2>
  <ul>
    <li>HTML-first rendering (zero JS by default)</li>
    <li>Partial hydration via islands</li>
    <li>Explicit client/server boundaries</li>
    <li>Frameworks used only where needed</li>
    <li>Performance-focused defaults</li>
  </ul>
</section>

<section class="app">
  <h3>Interactive Application (shared state)</h3>

  <div style="display:grid; gap:16px">
    <div>
      <h4 style="margin:0 0 8px">React island (client:load)</h4>
      <AppIsland client:load />
    </div>

    <div
      style="height: 420px; border: 1px dashed rgba(0,0,0,0.12); border-radius: 12px; display:flex; align-items:center; justify-content:center; color:#6b7280; font-size:14px;"
    >
      Scroll down to hydrate the Vue island (client:visible)
    </div>

    <div>
      <h4 style="margin:0 0 8px">Vue island (client:visible)</h4>
      <AppIslandVue client:visible />
    </div>
  </div>
</section>

<PerfIsland client:idle />

<style>
  :global(html) {
    font-family:
      Inter,
      ui-sans-serif,
      system-ui,
      -apple-system,
      Segoe UI,
      Roboto,
      Helvetica,
      Arial,
      "Apple Color Emoji",
      "Segoe UI Emoji";
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  :global(body) {
    margin: 0;
    background: #fbfbfc;
    color: #111827;
    line-height: 1.5;
  }

  :global(*) {
    box-sizing: border-box;
  }

  header {
    max-width: 1100px;
    margin: 32px auto 16px;
    padding: 0 16px;
  }

  header h1 {
    margin: 0 0 8px;
    font-size: 40px;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }

  .subtitle {
    margin: 0;
    color: #4b5563;
    font-size: 15px;
  }

  .explain {
    max-width: 1100px;
    margin: 16px auto;
    padding: 18px 16px;
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    background: #ffffff;
  }

  .explain h2 {
    margin: 0 0 10px;
    font-size: 18px;
    letter-spacing: -0.01em;
  }

  .explain ul {
    margin: 0;
    padding-left: 18px;
    color: #374151;
    font-size: 14px;
  }

  .explain li {
    margin: 6px 0;
  }

  .app {
    max-width: 1100px;
    margin: 16px auto 28px;
    padding: 18px 16px;
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    background: #ffffff;
  }

  .app h3 {
    margin: 0 0 12px;
    font-size: 16px;
    letter-spacing: -0.01em;
    color: #111827;
  }

  :global(.ag-theme-alpine) {
    font-family: inherit;
  }

  :global(.ag-theme-alpine .ag-header-cell-text) {
    font-weight: 600;
  }
</style>
